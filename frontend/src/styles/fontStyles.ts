import type { CSSProperties } from "react";

export const proximaFontStyle = {
  fontFamily: '"Proxima Nova", var(--font-geist-sans), "Helvetica Neue", Arial, sans-serif',
} satisfies CSSProperties;

export const rubikFontStyle = {
  fontFamily: 'var(--font-rubik), var(--font-geist-sans), "Helvetica Neue", Arial, sans-serif',
} satisfies CSSProperties;
